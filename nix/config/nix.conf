# Nix Configuration
# This directory (config/) is symlinked to ~/.config/nix/

# Enable Flakes and nix command
experimental-features = nix-command flakes

# Number of parallel build jobs (auto-detect CPU cores)
max-jobs = auto

# Don't show warnings for dirty git repos
warn-dirty = false
